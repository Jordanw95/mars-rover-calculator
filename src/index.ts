// Get command line arguments (excluding node and script path)
const args = process.argv.slice(2);

function main(args: string[]) {
    console.log("Received arguments:", args);
    // Add your logic here to handle the arguments
}

main(args);